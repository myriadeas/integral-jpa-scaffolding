#messages.properties
#foreach($entity in $entities)	
#${entity.name}
${entity.name}=${entity.humanName}
${entity.name}.title=${entity.humanName}
${entity.name}.create=Create ${entity.humanName} Form
${entity.name}.delete=Delete ${entity.humanName}
${entity.name}.edit=Edit ${entity.humanName} Form
${entity.name}.list=List Of ${entity.humanName}
${entity.name}.view=View ${entity.humanName}
${entity.name}.created=Successfully created ${entity.humanName}
${entity.name}.updated=Successfully updated ${entity.humanName}
${entity.name}.deleted=Successfully deleted ${entity.humanName}
${entity.name}.notfound=${entity.humanName} not found
${entity.name}.created.failed=Failed to create ${entity.humanName}
${entity.name}.deleted.failed=Failed to delete ${entity.humanName}
${entity.name}.updated.failed=Failed to update ${entity.humanName}
#foreach($attribute in $entity.attributes)
${entity.name}.${attribute.name}=${attribute.humanName}
${entity.name}.${attribute.name}.placeholder=${attribute.humanName.toLowerCase()}
#end
    
#end